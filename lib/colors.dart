import 'dart:ui';

import 'package:velocity_x/velocity_x.dart';

mixin Coolers {
  static const primaryColor = Color(0xffffffff);
  static const secondaryColor = Color(0x2fffffff);
  static const accentColor = Vx.blue700;
}